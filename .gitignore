# Rust
/target
**/*.rs.bk

# Frontend (Dioxus / dx)
crates/pnex-frontend/dist
crates/pnex-frontend/.dx
**/dist/

# Front — Tailwind v4 (CSS généré), bundle esptool-js et node_modules
crates/pnex-frontend/assets/tailwind.css
crates/pnex-frontend/assets/flasher.js
crates/pnex-frontend/node_modules/

# Environnement & secrets
.env
.env.*
!.env.example
*.age
secrets/

# Éditeurs
.idea/
.vscode/
*.swp

# Firmware PlatformIO (toolchain locale et caches de build pio)
firmware/.venv/
firmware/**/.pio/

# Outillage local
.task/
.claude/settings.local.json*
.claude/worktrees/

# Graphe de connaissances graphify (généré, projet et sous-dossiers)
graphify-out/

# Moteur de flow ETL (D18) — état du runtime (flows.json + runtime.json)
crates/pnex-backend/flow-state/
flow-state/
